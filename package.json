{
    "name": "gdpr",
    "private": "true",
    "workspaces": [
        "backend",
        "backend-auth",
        "frontend"
    ],
    "devDependencies": {
        "concurrently": "^9.2.1",
        "nodemon": "^3.1.10"
    },
    "scripts": {
        "dev": "concurrently -n backend,backend-auth,frontend -c blue,cyan,green \"npm run dev --workspace=backend\" \"npm run dev --workspace=backend-auth\" \"npm run dev --workspace=frontend\""
    }
}
